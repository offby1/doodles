"""You can't just run this; I haven't figured out how to make that work.
Instead:

    $ brew cask install anaconda
    $ /usr/local/anaconda3/bin/ipython3

... and then paste the below, one line at a time.

"""

get_ipython().run_line_magic('matplotlib', 'osx') # %matplotlib osx

import pandas as pd

df = pd.read_csv('/Users/erichanchrow/git-repositories/me/doodles/python/wind-weather/2017-data.csv')
df[['cloudCover', 'windBearing']].plot(y='cloudCover', x='windBearing', kind='scatter')
