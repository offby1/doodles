(module info (lib "infotab.ss" "setup")
  (define name "foof-loop")
  (define blurb
        (list "A Simple, Extensible Scheme Looping Facility"))
  (define primary-file "wrapper.ss")
  (define doc.txt "doc.txt")
  (define categories '(devtools)))